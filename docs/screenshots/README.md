# Screenshots

Place PNG captures here after a clean clone. Do not commit reports of real people.

The legacy localhost dashboard was removed in the 0.1.0b2 development milestone,
so the previous GUI capture list no longer applies. SPECTRE is CLI-first: capture
terminal output or a generated single-file HTML report instead.

Expected names — a local convention, not referenced by any document today. Neither
the root README nor the MkDocs site embeds screenshots, and no PNG is committed here.
The list is kept so captures land on predictable paths if the documentation starts
embedding them again:

- `cli-investigate.png`
- `cli-doctor.png`
- `report-html.png`
- `report-graph.png`
